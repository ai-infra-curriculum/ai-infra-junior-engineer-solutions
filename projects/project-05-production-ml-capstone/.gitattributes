# Git LFS Configuration for Production ML System
# Based on Module 003 Exercise 08 best practices

# ============================================================================
# PyTorch Models
# ============================================================================
*.pt filter=lfs diff=lfs merge=lfs -text
*.pth filter=lfs diff=lfs merge=lfs -text
*.ckpt filter=lfs diff=lfs merge=lfs -text
*.checkpoint filter=lfs diff=lfs merge=lfs -text

# ============================================================================
# TensorFlow Models
# ============================================================================
*.h5 filter=lfs diff=lfs merge=lfs -text
*.hdf5 filter=lfs diff=lfs merge=lfs -text
*.pb filter=lfs diff=lfs merge=lfs -text
*.keras filter=lfs diff=lfs merge=lfs -text
saved_model.pb filter=lfs diff=lfs merge=lfs -text

# TensorFlow SavedModel directories
**/saved_model/variables/* filter=lfs diff=lfs merge=lfs -text

# ============================================================================
# ONNX Models
# ============================================================================
*.onnx filter=lfs diff=lfs merge=lfs -text

# ============================================================================
# Serialized Models
# ============================================================================
*.pkl filter=lfs diff=lfs merge=lfs -text
*.pickle filter=lfs diff=lfs merge=lfs -text
*.joblib filter=lfs diff=lfs merge=lfs -text

# ============================================================================
# Model Weights & Parameters
# ============================================================================
*.bin filter=lfs diff=lfs merge=lfs -text
*.weights filter=lfs diff=lfs merge=lfs -text
*.params filter=lfs diff=lfs merge=lfs -text
*.safetensors filter=lfs diff=lfs merge=lfs -text

# Specific weight directories
weights/*.bin filter=lfs diff=lfs merge=lfs -text
models/production/**/*.pt filter=lfs diff=lfs merge=lfs -text
models/production/**/*.pth filter=lfs diff=lfs merge=lfs -text
models/production/**/*.h5 filter=lfs diff=lfs merge=lfs -text
models/production/**/*.onnx filter=lfs diff=lfs merge=lfs -text

# ============================================================================
# Large Data Files
# ============================================================================
# Note: Consider using DVC for datasets instead of Git LFS
# Uncomment if you need to track data in Git

# Parquet files
# *.parquet filter=lfs diff=lfs merge=lfs -text

# Feather files
# *.feather filter=lfs diff=lfs merge=lfs -text

# Arrow files
# *.arrow filter=lfs diff=lfs merge=lfs -text

# CSV files (only if large)
# data/raw/*.csv filter=lfs diff=lfs merge=lfs -text

# ============================================================================
# Pre-trained Model Files
# ============================================================================
# Hugging Face models
**/pytorch_model.bin filter=lfs diff=lfs merge=lfs -text
**/model.safetensors filter=lfs diff=lfs merge=lfs -text

# BERT/Transformer models
**/bert-*.bin filter=lfs diff=lfs merge=lfs -text
**/roberta-*.bin filter=lfs diff=lfs merge=lfs -text

# ============================================================================
# Other Large Files
# ============================================================================
# Archives
*.tar.gz filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text

# Docker images (if stored in repo)
*.tar filter=lfs diff=lfs merge=lfs -text

# ============================================================================
# Text Files (ensure proper line endings)
# ============================================================================
*.md text eol=lf
*.txt text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.json text eol=lf
*.py text eol=lf
*.sh text eol=lf
*.bash text eol=lf

# Kubernetes manifests
*.yaml text eol=lf

# Dockerfile
Dockerfile* text eol=lf

# ============================================================================
# Binary Files (mark as binary)
# ============================================================================
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary

# ============================================================================
# Usage Notes
# ============================================================================
# 1. Git LFS must be installed: brew install git-lfs (Mac) or apt-get install git-lfs (Linux)
# 2. Initialize LFS in repo: git lfs install
# 3. Track additional patterns: git lfs track "pattern"
# 4. View tracked files: git lfs ls-files
# 5. Check LFS status: git lfs status
#
# Best Practices:
# - Use Git LFS for models < 2GB
# - Use DVC for large datasets and model registries
# - Tag model versions: git tag -a model-v1.0.0 -m "Release v1.0.0"
# - Document model versions in MODELS.md
